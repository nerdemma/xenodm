# xenodm
Like unix based login manager custom config file. 
This config file is stored in the user main directory. 

files included at /home/$USER directory

/home/$USER directory
|_~/.Xsetup_0
|_~/.Xresources
setup files, required to install the files to user directory and check the packages required installed. 

 /setup
 |_ ./chk_pkg
 |_packageslist.txt
 |_./setup.sh


